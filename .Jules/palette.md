## 2026-02-12 - Navigation Accessibility
**Learning:** Many interactive elements (buttons, links) rely solely on visual cues (icons, colors) without semantic HTML attributes (`aria-label`, `aria-current`), making them inaccessible to screen readers.
**Action:** Always verify icon-only buttons have `aria-label` and active navigation states use `aria-current`.
